// This is the commands file that will store all commands available
// to enter in via the text interface

#pragma once

#include <vector>
#include <string>

std::vector<std::string> commands = {"ags", "blood", "chat", "clue_count", "dds", "ice", "record", "whip"};

int numCommands = commands.size();
